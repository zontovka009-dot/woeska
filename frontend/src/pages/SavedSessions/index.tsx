import {useEffect,useState} from 'react';import {useAuth} from '../../store/authStore';import {sessions} from '../../api/sessions';import {SessionCard} from '../../components/SessionCard';export default function SavedSessions(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{sessions.list().then(setItems).catch(()=>{})},[]);return <><h1>Сохранённые</h1><p className="muted">Последние 10 комнат, в которых ты был.</p>{items.map(x=><SessionCard key={x.id} session={x}/>)}{!items.length&&<div className="empty-card">Сессии появятся после создания комнат.</div>}</>}