import {create} from 'zustand'; import type {Room} from '../types/room'; type S={room:Room|null;setRoom:(room:Room)=>void;playing:boolean;position:number;setPlayback:(p:boolean,pos:number)=>void}; export const useRoom=create<S>(set=>({room:null,playing:false,position:0,setRoom:room=>set({room,playing:room.playing,position:room.position}),setPlayback:(playing,position)=>set({playing,position})}));