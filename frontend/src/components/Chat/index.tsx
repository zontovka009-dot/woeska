import {useState} from 'react'; import {sessions} from '../../api/sessions'; export function Chat({sessionId,userId,initial=[]}:any){const [items,setItems]=useState(initial),[text,setText]=useState('');async function send(){if(!text.trim())return;const m=await sessions.send(sessionId,{user_id:userId,text});setItems((x:any[])=>[...x,m]);setText('')}return <div className="chat"><div className="chat-list">{items.map((m:any)=><div className="msg" key={m.id}><b>{m.author_name}</b><p>{m.text}</p></div>)}</div><div className="chat-input"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Написать сообщение…"/><button onClick={send}>➤</button></div></div>}
